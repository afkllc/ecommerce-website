export function SiteFooter() {
  return (
    <footer className="border-t border-border/60 bg-card/40">
      <div className="mx-auto flex w-full max-w-6xl flex-col gap-3 px-6 py-6 text-sm text-muted-foreground sm:px-10 md:flex-row md:items-center md:justify-between">
        <p>AllPencils is a demo-ready storefront template built on Next.js and Medusa.</p>
        <p>Phase 2 now includes an anonymous cart and a simulated checkout built on the live Medusa store APIs.</p>
      </div>
    </footer>
  )
}
